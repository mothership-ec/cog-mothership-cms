<!DOCTYPE html>
<html>
	<head>
		<title>Search Results</title>
	</head>
	<body>
	{% for key,types in app.session.getFlashBag.all %}
		<div class="feedback {{ key }}">
			{% for message in types %}
				<p>{{ message|raw }}</p>
			{% endfor %}
		</div>
	{% endfor %}

	<h1>{% block heading %}Search Results{% endblock %}</h1>

	{% block content %}{% endblock %}

	</body>
</html>