{%  if hasPages == true %}

    {{ render(controller('Message:Mothership:CMS::Controller:ControlPanel:Search#index'))}}

{% endif %}

<a href="{{ url('ms.cp.cms.create')}}" class="button small create" data-live>{{ 'ms.cms.create.button.menu'|trans }}</a>

<ol class="menu">
    {{ _self.menu_links(tree, currentPageID) }}
</ol>

{% macro menu_links(links, currentPageID) %}
    {% for link in links %}
        <li {% if link.id == currentPageID %} class="active" {% endif %} >
            <a href="{{ url('ms.cp.cms.edit', {pageID: link.id}) }}" data-live>{{ link.title }}</a>
            {% if link.children %}
                <ol>
                    {{ _self.menu_links(link.children, currentPageID) }}
                </ol>
            {% endif %}
        </li>
    {% endfor %}
{% endmacro %}