twitter:
  name: # username
  slug: # slug to link to
facebook:
  name: # username
  slug: # slug to link to
instagram:
  name: # username
  slug: # slug to link to
pintrest:
  name: # username
  slug: # slug to link to