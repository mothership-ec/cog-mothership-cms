twitter:
  name: Mothership_Ecom
  slug: # slug to link to
facebook:
  name: mothership.ecommerce
  slug: # slug to link to
instagram:
  name: # instagram
  slug: # slug to link to
pinterest:
  name: # pinterest
  slug: # slug to link to
linked-in:
  name: # name
  slug: # slug to link to
tumblr:
  name: # name
  slug: # slug to link to
google-plus:
  name: # name
  slug: # slug to link to
soundcloud:
  name: # name
  slug: # slug to link to
bandcamp:
  name: # name
  slug: # slug to link to
youtube:
  name: # name
  slug: # slug to link to
vimeo:
  name: # name
  slug: # slug to link to
reddit:
  name: # name
  slug: # slug to link to
