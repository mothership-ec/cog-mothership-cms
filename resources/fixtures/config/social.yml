twitter:
  name: # username
  slug: # slug to link to
facebook:
  name: # username
  slug: # slug to link to
instagram:
  name: # username
  slug: # slug to link to
pinterest:
  name: # username
  slug: # slug to link to
